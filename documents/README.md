# documents/

- `cv-siham-larbi.pdf` — **not included yet.** Add your final CV PDF here with exactly
  this name, then edit `cv_pdf_href` near the top of `build.py`'s `build_cv()` function
  (or just search for `cv-siham-larbi.pdf`) if you want to rename it — then run
  `python3 build.py` again to rebuild the CV page's download button.
- The thesis (`memoire-larbi-rebhi-steganographie-generative.pdf`) and the four project
  report PDFs already here are the shared documents co-authored with Idir Rebhi during
  the joint Master's/Bachelor's coursework — copied over as-is.
